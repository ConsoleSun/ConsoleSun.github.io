<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>企业级 AI Agent 框架实践:我的外贸智能体系统</title>
      <link href="/2026/08/02/%E4%BC%81%E4%B8%9A%E7%BA%A7AI-Agent%E6%A1%86%E6%9E%B6%E5%AE%9E%E8%B7%B5-%E6%88%91%E7%9A%84%E5%A4%96%E8%B4%B8%E6%99%BA%E8%83%BD%E4%BD%93%E7%B3%BB%E7%BB%9F/"/>
      <url>/2026/08/02/%E4%BC%81%E4%B8%9A%E7%BA%A7AI-Agent%E6%A1%86%E6%9E%B6%E5%AE%9E%E8%B7%B5-%E6%88%91%E7%9A%84%E5%A4%96%E8%B4%B8%E6%99%BA%E8%83%BD%E4%BD%93%E7%B3%BB%E7%BB%9F/</url>
      
        <content type="html"><![CDATA[<blockquote><p>这不是一篇”Hello World”级别的 Agent 教程,而是一个真正跑在生产环境的外贸智能体系统的框架拆解。它同时服务<strong>外部客户</strong>(产品咨询、以图搜图)和<strong>内部员工</strong>(知识检索、邮件收发、客户挖掘、ERP 同步),并实现了全自动获客。</p></blockquote><h2 id="一、项目背景"><a href="#一、项目背景" class="headerlink" title="一、项目背景"></a>一、项目背景</h2><p>公司是一家玻璃瓶制造与出口企业(Valiant Glass)。业务上有两类典型诉求:</p><ul><li><strong>外部客户</strong>:想查”丘比特瓶子的规格””这款酒瓶有没有现货”,甚至直接发一张图纸问”能不能做”。</li><li><strong>内部员工</strong>:要查产品报价、生产工艺、管理制度,要发开发信、跟进客户、把客户同步进 ERP。</li></ul><p>传统做法是给业务人员配一堆系统 + 知识库,但知识分散、操作割裂。我做的这个系统用一个 <strong>Agent 统一承载两类场景</strong>,把知识检索、联网、邮件、ERP、视觉搜索全部收敛成”对话”这一种交互。</p><h2 id="二、核心设计理念"><a href="#二、核心设计理念" class="headerlink" title="二、核心设计理念"></a>二、核心设计理念</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Agent = LLM + 上下文 + 工具</span><br><span class="line">      = 大脑 + 眼睛 + 手脚</span><br></pre></td></tr></table></figure><p>这是整个系统的出发点。LLM 是大脑(推理决策),上下文是眼睛(它能看到什么),工具是手脚(它能做什么)。框架层的绝大多数工作,都是在把这三者的边界和配合打磨好。</p><h2 id="三、七层架构"><a href="#三、七层架构" class="headerlink" title="三、七层架构"></a>三、七层架构</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">┌─────────────────────────────────────────────┐</span><br><span class="line">│ ① 前端层      static/  客户/员工/管理/登录 4 个界面 │</span><br><span class="line">├─────────────────────────────────────────────┤</span><br><span class="line">│ ② API 网关层  api/      FastAPI + Uvicorn      │</span><br><span class="line">├─────────────────────────────────────────────┤</span><br><span class="line">│ ③ 认证鉴权层  auth/      JWT 双令牌 + 三级 RBAC    │</span><br><span class="line">├─────────────────────────────────────────────┤</span><br><span class="line">│ ④ 安全护栏层  sandbox/  输入过滤/输出过滤/速率限制   │</span><br><span class="line">├─────────────────────────────────────────────┤</span><br><span class="line">│ ⑤ Agent 核心层 agent/    ReAct 循环 + 工具网关      │</span><br><span class="line">├─────────────────────────────────────────────┤</span><br><span class="line">│ ⑥ 存储层      agent/storage/   Redis 会话/收件箱   │</span><br><span class="line">├─────────────────────────────────────────────┤</span><br><span class="line">│ ⑦ 可观测性层  agent/observability/  Trace + 评估   │</span><br><span class="line">└─────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure><p>七层各司其职,前后端可以完全解耦开发。<strong>核心技术资产集中在第⑤层</strong>,下面重点讲。</p><h2 id="四、Agent-核心层的四个关键机制"><a href="#四、Agent-核心层的四个关键机制" class="headerlink" title="四、Agent 核心层的四个关键机制"></a>四、Agent 核心层的四个关键机制</h2><h3 id="1-ReAct-循环-agent-loop-py"><a href="#1-ReAct-循环-agent-loop-py" class="headerlink" title="1. ReAct 循环(agent&#x2F;loop.py)"></a>1. ReAct 循环(agent&#x2F;loop.py)</h3><p>循环结构并不新奇:<code>LLM 决策 → 执行工具 → 结果喂回 → 再决策</code>。但实现上有两个工程化细节:</p><ul><li><strong>意图澄清预检</strong>:首轮先用一次轻量级 LLM 调用判断消息是否模糊。明确的(“VIT-001 的规格”)直接进入 ReAct;模糊的(“帮我查一下”)先返回一个精准的澄清提问,避免 Agent 在错误方向上白白消耗步数。</li><li><strong>最大步数护栏</strong>:默认 5 步封顶,防止 Agent 陷入循环烧 token。</li></ul><h3 id="2-上下文四层结构-harness-context-py"><a href="#2-上下文四层结构-harness-context-py" class="headerlink" title="2. 上下文四层结构(harness&#x2F;context.py)"></a>2. 上下文四层结构(harness&#x2F;context.py)</h3><p>上下文怎么组织,直接决定 Agent 的表现:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Layer 1: 静态 System Prompt   (角色 + 边界 + 工具描述 + 当前时间)</span><br><span class="line">Layer 2: 动态注入             (用户画像:名称/等级/偏好语言 + 发信/ERP 签名规约)</span><br><span class="line">Layer 3: 对话历史             (最近 20 轮,自动裁剪 + 澄清回应引导)</span><br><span class="line">Layer 4: 动态视角尾部注入     (PerspectiveFSM 驱动的 Recency Bias Tail Injection)</span><br></pre></td></tr></table></figure><p>值得一提的工程细节:为了最大化 <strong>DeepSeek 的上下文缓存命中率</strong>,系统把动态内容(时间戳、澄清引导、视角引导)统一折进用户消息末尾,让 system 前缀保持恒定。结果是缓存全程命中,速度显著提升。</p><h3 id="3-视角状态机-harness-perspective-fsm-py"><a href="#3-视角状态机-harness-perspective-fsm-py" class="headerlink" title="3. 视角状态机(harness&#x2F;perspective_fsm.py)"></a>3. 视角状态机(harness&#x2F;perspective_fsm.py)</h3><p>这是我觉得最有意思的设计。同一个 Agent 要面对<strong>客户</strong>和<strong>内部员工</strong>两类人,回答口吻、检索的库都该不一样。系统用一个轻量的状态机管理三种视角:</p><table><thead><tr><th>状态</th><th>触发场景</th><th>效果</th></tr></thead><tbody><tr><td><code>CUSTOMER_FACING_COMM</code></td><td>开发信、报价谈判、客户邮件</td><td>优先搜业务&#x2F;开发信库,外贸销售口吻</td></tr><tr><td><code>INTERNAL_OPERATIONAL</code></td><td>生产工艺、制度查询</td><td>优先搜管理&#x2F;技术库,内部口吻</td></tr><tr><td><code>NEUTRAL</code></td><td>日常问候、通用推理</td><td>等权搜索所有库</td></tr></tbody></table><p>三个关键机制:</p><ul><li><strong>防抖(Debounce)</strong>:NEUTRAL 状态下若已有上下文约束,不轻易退回上一状态,避免视角在几轮对话里反复横跳。</li><li><strong>Recency Bias Tail Injection</strong>:根据当前状态在上下文末尾动态注入行为引导——利用大模型对”末尾内容”更敏感的特性。</li><li><strong>RAG 权重分配</strong>:不同视角下动态调整 4 个知识库的检索优先级权重(0.5×~1.5×)。</li></ul><p>每个会话维护独立的 FSM 实例,状态跨轮次保持,配合有界的空闲淘汰策略防止内存无限增长。</p><h3 id="4-工具网关五步管线-agent-gateway"><a href="#4-工具网关五步管线-agent-gateway" class="headerlink" title="4. 工具网关五步管线(agent&#x2F;gateway&#x2F;)"></a>4. 工具网关五步管线(agent&#x2F;gateway&#x2F;)</h3><p>这是系统的”安全阀”。每次工具调用都走同一条 5 步管线:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">熔断 → 权限校验 → 工具执行 → 结果校验 → 失败修正</span><br></pre></td></tr></table></figure><ul><li><strong>熔断</strong>(breaker.py):全局连续 3 次失败 → 60s 冷却,防止雪崩。</li><li><strong>权限</strong>(permissions.py):三级 RBAC(customer 5 个工具 &#x2F; staff 13 个 &#x2F; admin 13 个)+ 参数级规则。比如客户永远调不到 <code>send_email</code>。</li><li><strong>执行</strong>(gateway.py):从注册表取工具 spec,调用异步执行函数。工具用 <code>@tool()</code> 装饰器声明,天然可注册、可发现。</li><li><strong>校验</strong>(validators.py):每个工具配独立校验器,检测空结果&#x2F;错误&#x2F;异常。</li><li><strong>修正</strong>(gateway.py::_correct()):校验失败 → 生成纠偏上下文反馈给 LLM,让它换一种方式重试(同工具最多 2 次)。</li></ul><p>这一条管线解决了 Agent 落地最常见的痛点:<strong>工具不可靠、结果不可信、失败不可控</strong>。</p><h2 id="五、安全设计-五面防御"><a href="#五、安全设计-五面防御" class="headerlink" title="五、安全设计:五面防御"></a>五、安全设计:五面防御</h2><p>生产环境的 Agent 最怕两件事:<strong>被人诱导越权</strong> 和 <strong>幻觉乱说</strong>。系统在 5 个面做了防御:</p><ol><li><strong>输入安全</strong>:20+ 条正则检测提示注入(中英文覆盖),拦截角色标签注入、越狱模式、指令覆盖。</li><li><strong>输出安全</strong>:正则检测 API Key 泄露、内网 IP 泄露、内部价格泄露;外加可选的 <strong>LLM 二次幻觉校验</strong>(检测编造 SKU、虚构报价)。</li><li><strong>工具安全</strong>:SSRF 防护(<code>web_fetch</code> 阻止内网&#x2F;本地地址)、路径穿越防护(<code>save_file</code> 和图纸代理均做路径校验)、文件大小限制。</li><li><strong>认证鉴权</strong>:JWT access + refresh 双令牌(刷新一次性轮转)、API Key bcrypt 哈希存储、三级 RBAC。</li><li><strong>速率限制</strong>:Redis 滑动窗口(默认 10 次&#x2F;分钟)+ 日配额(200 次&#x2F;天)。</li></ol><h2 id="六、可观测性与评估"><a href="#六、可观测性与评估" class="headerlink" title="六、可观测性与评估"></a>六、可观测性与评估</h2><ul><li><strong>Trace 追踪</strong>:每个请求生成一条结构化 JSONL 日志,记录 <code>trace_id</code>、<code>llm_calls</code>、<code>tool_calls</code>、每步耗时 span。线上排查 Agent 行为靠的就是它。</li><li><strong>评估框架(agent&#x2F;eval&#x2F;)</strong>:LLM-as-Judge 从 4 个维度打分——关键词覆盖度、幻觉检测(保守原则,宁可漏判不误判)、澄清检测、综合评分。测试集跑完后生成 Markdown 报告,和 <code>baseline.json</code> 对比做回归。</li></ul><blockquote><p>Agent 系统没有评估框架 &#x3D; 盲人骑瞎马。哪怕是一个 4 维的粗粒度打分,也能让你在改 prompt、改工具后立刻知道是变好还是变坏。</p></blockquote><h2 id="七、特色功能-全自动获客-Auto-Campaign"><a href="#七、特色功能-全自动获客-Auto-Campaign" class="headerlink" title="七、特色功能:全自动获客(Auto Campaign)"></a>七、特色功能:全自动获客(Auto Campaign)</h2><p>这是系统里最能体现”工具链编排”价值的场景:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">后台一键 → 按配置逐国找客户 → 已联系邮箱/域名去重</span><br><span class="line">  → LLM 起草开发信(先查开发信知识库,失败回退模板)</span><br><span class="line">  → 员工个人 SMTP 发送(限速防频控)</span><br><span class="line">  → 自动同步 ERP</span><br></pre></td></tr></table></figure><p>几个工程细节:</p><ul><li><strong>去重</strong>:Redis SET 记录所有已联系邮箱&#x2F;域名,跨国家重复公司不重发。</li><li><strong>进度流独立存储</strong>:campaign 进度放独立 Redis store,<strong>绝不进入 LLM 上下文</strong>,防止获客过程污染对话。</li><li><strong>可停止可恢复</strong>:每个用户一个后台 task,随时可停。</li></ul><h2 id="八、工程经验小结"><a href="#八、工程经验小结" class="headerlink" title="八、工程经验小结"></a>八、工程经验小结</h2><ol><li><strong>把”动态”和”静态”分离</strong>:System Prompt 保持恒定以命中上下文缓存,动态内容全进用户消息末尾——一次改动,速度肉眼可见提升。</li><li><strong>Agent 的安全必须做成管线,而不是散落的 if</strong>:5 步网关让每个新工具自动获得熔断、权限、校验能力。</li><li><strong>状态机比”一股脑灌上下文”更优雅</strong>:视角 FSM 用很小的代价让同一个模型适配了两种截然不同的用户。</li><li><strong>能交给确定性逻辑的就别让 LLM 干</strong>:去重用 Redis、限速用滑动窗口、幻觉检测兜底用正则先行——LLM 只做它擅长的事。</li></ol><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>这套框架最有价值的地方,不是某个单点技术,而是把 <strong>LLM 的不确定性</strong> 用工程手段约束起来:上下文怎么给、工具怎么调用、失败怎么修正、输出怎么过滤、质量怎么评估——每个环节都有明确的设计。</p><p>AI Agent 的落地,拼的不是模型的智商,而是<strong>框架对边界的定义和兜底能力</strong>。</p><hr><p><em>如果你对这个项目感兴趣,源码在 <a href="https://github.com/ConsoleSun/trade-agent">github.com&#x2F;ConsoleSun&#x2F;trade-agent</a>。</em></p>]]></content>
      
      
      <categories>
          
          <category> 技术 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI Agent </tag>
            
            <tag> LLM </tag>
            
            <tag> 架构设计 </tag>
            
            <tag> 外贸数字化 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
